/** Generator-Ergebnis als Default-Export: drei geseedete Felsen (Gruppe = Ordnername `natur`). */
import { felsGenerator } from '../../generatoren';

export default felsGenerator.generate(7, { anzahl: 3, groesse: 12 });
