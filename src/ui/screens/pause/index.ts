/** Pause menu (M3-31): the menu, its hooks into the page and the settings it offers until the settings screen (M7-55). */
export { PauseMenu, type PauseMenuProps } from './PauseMenu';
export type { MenuHooks, SaveOutcome } from './hooks';
export { PAUSE_SETTING_ROWS, rangeValues, stepValue, type SettingRow } from './settingsRows';
